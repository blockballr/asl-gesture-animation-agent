# Gemini API key for the English->ASL gloss proxy (server-side only).
# Local dev: copy this file to `.env` and paste your key. `.env` is gitignored.
# Production (Vercel): set GEMINI_API_KEY in Project Settings -> Environment Variables.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
